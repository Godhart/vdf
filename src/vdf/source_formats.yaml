__fallback__:
  # Fallback spec, based on markdown
  ext             : null
  kind            : text
  spec:
    escape_symbol : null
    fenced_seq    : "```"
    fenced_regex  : "^(`{3,})([\\w-]+)?$" # TODO: read fenced spec carefully
    cells_split   : ["", "---", ""]
    frontmatter   : True

markdown:
  ext             : [".md"]
  kind            : text
  spec            : {} # NOTE: spec would be same as fallback
